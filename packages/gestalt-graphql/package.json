{
  "name": "gestalt-graphql",
  "version": "0.0.20",
  "description": "generates a GraphQL schema with resolution based on a definition using the GraphQL schema language.",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/charlieschwabacher/gestalt.git"
  },
  "scripts": {
    "test": "mocha",
    "build": "babel src --out-dir lib/",
    "watch": "babel --watch src --out-dir lib/"
  },
  "author": "Charlie Schwabacher",
  "license": "ISC",
  "dependencies": {
    "camel-case": "^3.0.0",
    "constant-case": "^2.0.0",
    "gestalt-utils": "^0.0.20",
    "graphql": "^0.6.0",
    "graphql-relay": "^0.4.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0",
    "mocha": "^2.5.3"
  }
}
