// @flow
// the '@virtual' and '@relationship' directives are incompatible with any other
// directives

import type {Document} from 'gestalt-utils';

export default function incompatibleDirectivesCannotBeComibined(
  ast: Document,
): void {

}
