// @flow
// directives can only be applied to object types extending the node interface

import type {Document} from 'gestalt-utils';

export default function directivesMustApplyToCompatibleTypes(
  ast: Document,
): void {

}
