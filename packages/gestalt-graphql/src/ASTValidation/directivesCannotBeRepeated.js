// @flow
// all directives defined by Gestalt can only be applied once to a type or field

import type {Document} from 'gestalt-utils';

export default function directivesCannotBeRepeated(ast: Document): void {

}
