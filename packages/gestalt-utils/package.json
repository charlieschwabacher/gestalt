{
  "name": "gestalt-utils",
  "version": "0.0.17",
  "description": "Shared dependencies for the gestalt packages",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/charlieschwabacher/gestalt.git"
  },
  "scripts": {
    "test": "mocha",
    "flow": "flow; test $? -eq 0 -o $? -eq 2",
    "build": "babel src --out-dir lib/ && npm run build-dot-flow",
    "build-dot-flow": "find ./src -name '*.js' | while read filepath; do cp $filepath `echo $filepath | sed 's/\\\/src\\\//\\\/lib\\\//g'`.flow; done",
    "watch": "babel --watch src --out-dir lib/"
  },
  "author": "Charlie Schwabacher",
  "license": "MIT",
  "dependencies": {
    "camel-case": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "dataloader": "^1.2.0",
    "graphql": "^0.6.0",
    "graphql-relay": "^0.4.2"
  }
}
