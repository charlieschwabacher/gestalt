{
  "name": "gestalt-postgres",
  "version": "0.0.1",
  "description": "PostgresQL database adapter for the gestalt framework",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/charlieschwabacher/gestalt.git"
  },
  "scripts": {
    "test": "mocha",
    "flow": "flow; test $? -eq 0 -o $? -eq 2",
    "build": "babel src --out-dir lib/",
    "watch": "babel --watch src --out-dir lib/"
  },
  "author": "Charlie Schwabacher",
  "license": "MIT",
  "dependencies": {
    "change-case": "^2.3.1",
    "dataloader": "^1.2.0",
    "pg": "^4.5.6",
    "pluralize": "^2.0.0",
    "gestalt-utils": "^0.0.1"
  },
  "devDependencies": {
    "babel-polyfill": "^6.7.2",
    "babel-register": "^6.7.2",
    "colors": "^1.1.2",
    "gestalt-graphql": "^0.0.1",
    "mocha": "^2.5.3"
  }
}
