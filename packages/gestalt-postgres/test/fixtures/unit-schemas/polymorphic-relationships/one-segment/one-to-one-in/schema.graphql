type User implements Node {
  id: ID!
  image: Image @relationship(path: "-INSPIRED->")
}
type Post implements Node {
  id: ID!
  image: Image @relationship(path: "-INSPIRED->")
}
type Image implements Node {
  id: ID!
  subject: Subject @relationship(path: "<-INSPIRED-")
}
union Subject = User | Post
