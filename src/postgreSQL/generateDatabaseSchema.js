// Generates an internal representation of a PostgreSQL schema from a GraphQL
// type definition AST.
// @flow

import {} from 'graphql/language/ast';
import {DatabaseSchema} from './types';

export default function generateDatabaseSchema(ast: Object): DatabaseSchema {
  return {};
}
